# GPUmatrix 1.0.3

* Fixed bug in `diag()` method for `gpu.matrix.torch` objects.
* Added documentation for `dimnames<-` method for `gpu.matrix.torch`.
* Fixed Rd cross-references for `Matrix-class` and `%&%`.
* Updated URLs.
